#include <bits/stdc++.h>
using namespace std;
int main()
{

    // cout << "enter the size \n";
    // int size;
    // cin >> size;
    // cout<<"Enter the arrays element \n";
    // int arr[size];
    // for (int i = 0; i < size; i++)
    // {
    //     cin >> arr[i];
    // }
    // cout << "printing the arrays element ";
    // for (int i = 0; i < size; i++)
    // {
    //     cout << arr[i] << " ";
    // }
    // cout << "enter the indexes which I want to swap ";
    // int index1, index2;
    // cin >> index1 >> index2;
    // if (index1 < 0 || index2 > size || index2 < 0 || index2 > size)
    // {
    //     cout << "Index is out of the array or invalid index \n";
    // }
    // else
    // {
    //     int temp = arr[index1];
    //     arr[index1] = arr[index2];
    //     arr[index2] = temp;
    // }
    // cout << "printing the arrays element after swapping ";
    // for (int i = 0; i < size; i++)
    // {
    //     cout << arr[i] << " ";
    // }

    // strings, json1 and json2, find the list of keys for which the values are different.
    

    return 0;
}