#include<stdio.h>

int main(){
    /*
    Given a string num that contains only digits and an integer target, return all 
possibilities to insert the binary operators +', , and/or * between the digits of 
num so that the resultant expression evaluates to the target value. 
Note that operands in the returned expressions should not contain leading zeros. 
For example, if Input: num = "232", target = 8, the Output: ["2 *3+2", "2+3*2"] 
    */
   char num[] = "232";
    int target = 8;
    int n = 3;
     for 
    return 0;
}