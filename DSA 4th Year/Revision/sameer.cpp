#include <bits/stdc++.h>
using namespace std;

void kidzone(int N, int A[], int Q, int X[]) {
    // int results[Q]; // Create an array to store the results for each day

    // for (int q = 0; q < Q; q++) {
    //     int counter = 0;
    //     for (int i = 0; i < N; i++) {
    //         if (X[q] >= A[i]) {
    //             counter++;
    //         }
    //     }
    //     results[q] = counter;
    // }

    // for (int q = 0; q < Q; q++) {
    //     cout << results[q] << endl; // Print the result for each day
    // }
    // // return 0; // Return 0 as this function doesn't need to return a result
if(1){
    cout<<"1"<<endl;
    return;
    // break;
}
    
}

int main() {
    int N = 4;
    int A[] = {100, 12, 321, 112};
    int Q = 5;
    int X[] = {10, 50, 62, 125, 500};
    kidzone(N, A, Q, X);
    return 0;
}
